import ppb


def main():
    ppb.run(resolution=(800, 800))
    # scene_params = {"background_color": (0, 0, 0)}
    # with GameEngine(BaseScene,
    #                 resolution=(800, 800),
    #                 scene_params=scene_params
    #                 ) as engine:
    #     engine.run()


if __name__ == "__main__":
    main()
